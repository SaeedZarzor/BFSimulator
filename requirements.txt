# Brain-Folding Simulator — GUI runtime dependencies.
# The interface is now a single PySide6 app (BFSimulator.py + bf_fields.py +
# bf_runner.py + bf_results.py). It replaced the old customtkinter/tkinter/PIL/
# OpenCV/PySimpleGUI/psutil stack.
#
# Note: the ParaView post-processing scripts (*_video_*.py, final_folding_*.py)
# run under ParaView's own bundled `pvpython`, not this environment, so their
# numpy/paraview imports are provided by ParaView and are intentionally not
# listed here.

PySide6>=6.5

# Optional — only needed to repackage the app as a macOS .app bundle:
# py2app>=0.28
